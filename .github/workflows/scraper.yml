name: "CyberPulse Telegram News Scraper"

on:
  schedule:
    # Runs every 30 minutes
    - cron: '*/30 * * * *'
  workflow_dispatch: # Allows manual run and external webhook trigger

jobs:
  scrape-and-send:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 1

      - name: Run Scraper and Dispatch to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          FILTER_MODE: ${{ vars.FILTER_MODE || 'all' }}
          MAX_SEND: ${{ vars.MAX_SEND || '6' }}
        run: |
          python3 scraper.py

      - name: Commit & Push History (Prevent Duplicates)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add sent_history.json || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: update sent news history [skip ci]" && git pull --rebase origin main && git push) || true
